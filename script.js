const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);window.addEventListener('load',()=>$('.loader')?.classList.add('done'));$('#year').textContent=new Date().getFullYear();const nav=$('nav'),menu=$('.menu');menu?.addEventListener('click',()=>{nav.classList.toggle('open')});$$('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});$$('.reveal').forEach(e=>io.observe(e));const hero=$('.hero'),bg=$('.hero-bg');hero?.addEventListener('mousemove',e=>{const x=(e.clientX/innerWidth-.5),y=(e.clientY/innerHeight-.5);bg.style.transform=`scale(1.06) translate(${x*-12}px,${y*-8}px)`});hero?.addEventListener('mouseleave',()=>bg.style.transform='scale(1.05)');let last=0;addEventListener('scroll',()=>{const y=scrollY;if(bg)bg.style.backgroundPosition=`center calc(50% + ${Math.min(y*.08,60)}px)`;last=y},{passive:true});const cursor=$('.cursor');addEventListener('mousemove',e=>{if(cursor){cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px'}});$$('a,button').forEach(el=>{el.addEventListener('mouseenter',()=>cursor?.classList.add('active'));el.addEventListener('mouseleave',()=>cursor?.classList.remove('active'))});
